sudo: true

language: python
python:
  - "3.5"


install:

  - pip install coveralls
  - pip install -r requirements.txt

# command to run tests
script: nosetests --with-coverage --cover-package=v1,v2

after_success:
  - coveralls

notifications:
  slack:
    on_success: never
    on_failure: never
  email: false




